<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team.dev.helpy.rame">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_icon2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Splash_screen"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".mainmenu" />
        <activity
            android:name=".menubeta"
            android:label="@string/title_activity_menubeta" />
        <activity android:name=".Desc.keraton" />
        <activity android:name=".Maps.MapsTebing" />
        <activity android:name=".Desc.lereng" />
        <activity android:name=".Maps.Maps_lereng" />
        <activity android:name=".Desc.tahura" />
        <activity android:name=".Maps.Maps_tahura" />
        <activity android:name=".Desc.moko" />
        <activity android:name=".Maps.Maps_moko" />
        <activity android:name=".Desc.lawang" />
        <activity android:name=".Maps.Maps_lawang" />
        <activity android:name=".Desc.floating" />
        <activity android:name=".Maps.Maps_floating" />
        <activity android:name=".Desc.stone" />
        <activity android:name=".Maps.Maps_stone" />
        <activity android:name=".Desc.dpakar" />
        <activity android:name=".Maps.Maps_dpakar" />
        <activity android:name=".Desc.dago" />
        <activity android:name=".Maps.Maps_dago" />
        <activity android:name=".Desc.kambing" />
        <activity android:name=".Maps.Maps_kambing" />
    </application>

</manifest>